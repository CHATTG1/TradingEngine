topologyName=TradingEngine
killIfRunning=true
zk.hosts=hdp.bridge:2181
numWorkers=1
localMode=true

driverClassName=org.apache.phoenix.jdbc.PhoenixDriver
jdbcUrl=jdbc:phoenix:hdp.bridge:2181:/hbase-unsecure
dataSource.user=
dataSource.password=

# Kafka Spout config
raw.topic=quotes
raw.consumerGroupID=quotes
